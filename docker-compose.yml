version: "3"

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    depends_on:
      - "backend"

  backend:
    build:
      context: ./server
    ports:
      - "3000:3000"

  # postgres:
  #   image: postgres:latest
  #   env_file:
  #     - .env
  #   ports:
  #     - "5432:5432"
